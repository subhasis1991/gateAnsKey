<snippet>
    <content><![CDATA[
app.factory('${1:Contact}', ['${2:$resource}', function(${2:$resource}){
    return $resource('${3:/api/user/:id}', {${4:id:'@id'}},{
            update: {'method': 'PUT'}
    });
}]);
]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>ag-resource</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <!-- <scope>source.python</scope> -->
</snippet>
